import { SearchkatedraPipe } from './searchkatedra.pipe';

describe('SearchkatedraPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchkatedraPipe();
    expect(pipe).toBeTruthy();
  });
});
