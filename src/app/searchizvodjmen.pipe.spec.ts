import { SearchizvodjmenPipe } from './searchizvodjmen.pipe';

describe('SearchizvodjmenPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchizvodjmenPipe();
    expect(pipe).toBeTruthy();
  });
});
