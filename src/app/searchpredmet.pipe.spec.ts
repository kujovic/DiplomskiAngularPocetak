import { SearchpredmetPipe } from './searchpredmet.pipe';

describe('SearchpredmetPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchpredmetPipe();
    expect(pipe).toBeTruthy();
  });
});
