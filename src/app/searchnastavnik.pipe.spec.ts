import { SearchnastavnikPipe } from './searchnastavnik.pipe';

describe('SearchnastavnikPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchnastavnikPipe();
    expect(pipe).toBeTruthy();
  });
});
